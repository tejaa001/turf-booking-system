# Git version control files
.git
.gitignore

# Dependencies are installed within the container, so we don't need the local copy.
node_modules

# Environment files should be injected at runtime, not built into the image.
.env
.env.*
.env.example

# Docker-related files are not needed inside the image.
Dockerfile
docker-compose.yml

# Logs, documentation, and other non-essential files
npm-debug.log
README.md
docs/